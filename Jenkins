pipeline {
    agent {
        dockerfile true
    }
    stages{
        stage('Run App') {
            steps {
                sh 'python ./app.py'
        }
    }
        stage('Test with E2E') {
            steps {
                sh 'python ./tests/E2E.py'
            }
        }
    }
}
